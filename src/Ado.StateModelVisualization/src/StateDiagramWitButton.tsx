import React from 'react';
import ReactDOM from 'react-dom';
import StateDiagramWitButton from './components/StateDiagramWitButton';

// Mount the React component when the DOM is loaded
ReactDOM.render(<StateDiagramWitButton />, document.getElementById('root'));