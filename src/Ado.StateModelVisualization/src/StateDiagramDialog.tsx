import React from 'react';
import ReactDOM from 'react-dom';
import StateDiagramDialog from './components/StateDiagramDialog';

// Mount the React component when the DOM is loaded
ReactDOM.render(<StateDiagramDialog />, document.getElementById('root'));